<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ShoppingList" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ShoppingList" parent="Base.Theme.ShoppingList" />
    <style name="CardView_Enabled." parent="CardView">
        <item name="android:foreground">
            @drawable/gradient_item_enabled
        </item>
    </style>

    <style name="CardView_Disabled." parent="CardView">
        <item name="android:foreground">
            @drawable/gradient_item_disabled
        </item>
    </style>
</resources>